public class Main {
    public static void main(String[] args) {
        double springCosts = 193.72;
        double summerCosts = 315.45;
        double fallCosts = 219.32;
        double winterCosts = 267.9;
        double totalCosts = springCosts + summerCosts + fallCosts + winterCosts;
        System.out.println("Your maintenance costs for Spring($" + springCosts + ") Summer($" + summerCosts + ") Fall($" + fallCosts + ") and Winter($" + winterCosts + ") total to a yearly maintenance cost of $" + totalCosts);
    }
}